# Version floors, not hard pins: Streamlit Community Cloud fixes the Python
# version at deploy time (this app runs on <= 3.10), so the deps must resolve
# across Python versions. Two constraints matter:
#   - shap >= 0.46 parses the bracketed base_score ("[5E-1]") emitted by
#     xgboost >= 2.1; older shap raised "could not convert string to float".
#   - xgboost >= 2.1 so the committed model (saved by a 3.x xgboost) loads.
# CI exercises Python 3.10 / 3.11 / 3.12 to keep these floors honest.
xgboost>=2.1,<4
shap>=0.46
streamlit>=1.35.0
pandas>=2.0.0
numpy>=1.26.0
scikit-learn>=1.4.0
plotly>=5.20.0
pyarrow>=14.0.0
