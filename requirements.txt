# Pinned, reproducible set. Requires Python 3.11+ (deploy the Community Cloud
# app on 3.11). The xgboost/shap pair must stay in lockstep: xgboost >= 3.0
# serialises base_score as a list ("[5E-1]") and only shap >= 0.50 parses it,
# and shap >= 0.50 itself requires Python 3.11+. On Python 3.10 this combination
# is unsatisfiable, which is why the runtime is 3.11+.
xgboost==3.2.0
shap==0.51.0
streamlit==1.59.2
pandas==3.0.3
numpy==2.4.6
scikit-learn==1.9.0
plotly==6.9.0
pyarrow==24.0.0
